---
summary: "CLI reference for `anvika clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `anvika clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `anvika clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `anvika clawbot qr` (same behavior as [`anvika qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `anvika clawbot qr` -> `anvika qr`
