---
summary: "CLI reference for `anvika reset` (reset local state/config)"
read_when:
  - You want to wipe local state while keeping the CLI installed
  - You want a dry-run of what would be removed
title: "reset"
---

# `anvika reset`

Reset local config/state (keeps the CLI installed).

```bash
anvika reset
anvika reset --dry-run
anvika reset --scope config+creds+sessions --yes --non-interactive
```
